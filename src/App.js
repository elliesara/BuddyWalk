import Front from "./pages/Front"
import './App.css';

function App() {
  return (
    // this is likely going to be a routing thing (if logged in, go to front, if not go to register/login pages) 
    // i just have front here as a placeholder
    <Front />
  );
}

export default App;
